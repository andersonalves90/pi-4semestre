<manifest xmlns:android="http://schemas.android.com/apk/res/android"
package="jumpers.delta.sistemasparainter.net.appdelta">

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.CAMERA" />

<application android:allowBackup="true" android:icon="@mipmap/ic_launcher" android:label="@string/app_name"
    android:supportsRtl="true" android:theme="@style/AppTheme">

    <activity android:name=".LoginActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <!-- <activity android:name=".LoginActivity"></activity> -->
     <activity android:name=".MenuActivity"></activity> -->

     <activity android:name=".CadastroActivity"></activity>

     <activity android:name=".CadastroEnderecoActivity"></activity>

     <activity android:name=".DetalheActivity"></activity>

     <activity android:name=".FinalizacaoActivity"></activity>

     <activity android:name=".PagamentoActivity"></activity>
     <activity android:name=".QRCode"/>

 </application>

 </manifest>

